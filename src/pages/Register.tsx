import FormPopup from "../components/FormPopup";

const Register = () => {
  return (
    <section className="register-page">
      <FormPopup page="register" />
    </section>
  );
};

export default Register;
